spring:
  application:
    name: eureka
  cloud:
    config:
      uri: http://config:8888
      retry:
        maxAttempts: 1
      failFast: true
      label: master
eureka:
  client:
    register-with-eureka: false
